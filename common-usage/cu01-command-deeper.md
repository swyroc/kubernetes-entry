# kubectl run
```
Deploy an application to Kubernetes cluster, by deploying an image include the application source code to kubernetes cluster you can run an image quickly.

```

# kubectl expose
```
Application is running locally in Kubernetes cluster, by running this command Kubernetes will expose an application to let user request. 
```

